{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "chrome-events",
  "description": "Let each window's local chrome sidebar listen for config-reload/badge events. The chrome is each window's local (App-URL) main webview; content tabs load remote (External) URLs, and this capability has no `remote` block (local-only, the default), so Tauri denies core:event to remote content by origin regardless of label — the sidebar gets events, remote pages never do.",
  "windows": ["*"],
  "webviews": ["*"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
